// Single source of truth for the GitHub Pages project path
// (https://<user>.github.io/ooga-tabooga). Used by next.config.ts (basePath)
// and by anything that talks to a public/ asset by absolute URL, since those
// aren't rewritten by Next's basePath handling.
export const BASE_PATH = "/ooga-tabooga";
